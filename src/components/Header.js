import styled from 'styled-components';

const HeaderContainer = styled.div`
  color: white;
  height: 100px;
  display: flex;
`

export const Header = () => {
  return (
    <HeaderContainer>
      Header
    </HeaderContainer>
  )
};